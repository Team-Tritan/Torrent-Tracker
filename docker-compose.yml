services:
  tracker:
    build: .
    network_mode: host
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
    volumes:
      - ./src:/app/src
      - ./data:/app/data
